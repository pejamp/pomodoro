import styled from "styled-components";

export const TimeStyles = styled.span`
  color: var(--white-900);
  font-size: 7rem;
  font-weight: 700;
  text-align: center;

  @media screen and (min-width: 501px) {
    font-size: 10rem;
  }
`; 